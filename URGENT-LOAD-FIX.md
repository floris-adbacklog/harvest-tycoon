Fixed: Welcome Back read the initial payload after it was cleared. The welcome summary is now captured first and shown only after farm readiness. UI setup exceptions now use the existing error screen. Replace public/game.js and redeploy the frontend. The Supabase backend was restored to the previous code (version 66); the event progress trigger remains paused during recovery. Account eligibility column permissions have been corrected. Do not rerun feature rollout SQL during this incident.
