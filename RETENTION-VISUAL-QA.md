# Retention visual QA

Playwright with headless Chromium rendered the new events, daily Family sharing, Welcome Back and existing-admin extension at 390px and 1280px viewport widths. Both fixture runs passed without JavaScript page errors or admin dialog horizontal overflow. The generated flag was visually checked against existing artwork and the mobile event card. Long event eligibility rules are collapsible. The admin form scrolls vertically on mobile.

These are local fixtures using the project's actual CSS and feature modules, with mocked API responses. No real rewards were issued. A signed-in staging/production smoke test and physical-device check remain necessary before release.
